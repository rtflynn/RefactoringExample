/* 
Based on the main example from the book by Martin Fowler and Kent Beck.
Hopefully I remember to snapshot enough to capture the main refactorings.
*/

#include "render_statement.h"


int main()
{
    std::cout << statement(example) << std::endl;
}
